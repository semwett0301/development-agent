[project]
name = "webhook-service"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.34",
    "aiokafka>=0.12",
    "pydantic>=2.10",
    "pydantic-settings>=2.7",
]